// Navbar component removed: language dropdown moved into HeroSection.
// Keeping a small stub export in case other modules still import it.
import React from 'react';
export const Navbar = () => null;
